package com.newlang.backend.enums;

// Estado de la palabra o expresión que se está aprendiendo (LEARNING), si está pendiente (PEDNING), aprendiendo o ya ha adquirido el conocimiento (MASTERED)
public enum Status {
    PENDING,
    LEARNING,
    MASTERED
}
